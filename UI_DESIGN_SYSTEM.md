https://github.com/Nasipbek2003/Bilimpoz-teacher.githttps://github.com/Nasipbek2003/Bilimpoz-teacher.githttps://github.com/Nasipbek2003/Bilimpoz-teacher.githttps://github.com/Nasipbek2003/Bilimpoz-teacher.githttps://github.com/Nasipbek2003/Bilimpoz-teacher.git# Документация: Дизайн-система и UI компоненты Bilimpoz Admin

## Обзор

Данный документ описывает полную реализацию дизайн-системы, всех UI компонентов, функциональности, стилей и элементов интерфейса административной панели Bilimpoz Admin.

---

## 1. Дизайн-система

### 1.1 Цветовая палитра

#### Основные цвета

| Цвет | Hex | Использование | Tailwind класс |
|------|-----|---------------|----------------|
| **Фон страницы** | `#0b0b0b` | Основной фон приложения | `bg-[#0b0b0b]` |
| **Фон карточек** | `#151515` | Фон модалок, карточек, сайдбара | `bg-[#151515]` |
| **Фон элементов** | `#242424` | Фон кнопок, инпутов, выпадающих меню | `bg-[#242424]` |
| **Основной текст** | `#ffffff` | Белый текст | `text-white` |
| **Вторичный текст** | `#9ca3af` | Серый текст (gray-400) | `text-gray-400` |
| **Границы** | `#404040` | Границы элементов | `border-gray-600` |
| **Границы при hover** | `#606060` | Границы при наведении | `border-gray-500` |

#### Акцентные цвета

| Цвет | Hex | Использование | Tailwind класс |
|------|-----|---------------|----------------|
| **Синий** | `#3B82F6` | Информация, ссылки | `text-blue-400`, `bg-blue-500/10` |
| **Зеленый** | `#10B981` | Успех, активные элементы | `text-green-400`, `bg-green-500/10` |
| **Красный** | `#EF4444` | Ошибки, удаление | `text-red-400`, `bg-red-500/10` |
| **Желтый** | `#F59E0B` | Предупреждения | `text-yellow-400`, `bg-yellow-500/10` |
| **Фиолетовый** | `#8B5CF6` | Премиум, особые элементы | `text-purple-400`, `bg-purple-500/10` |
| **Оранжевый** | `#F97316` | Дополнительные акценты | `text-orange-400`, `bg-orange-500/10` |

#### Цвета для статусов

| Статус | Фон | Текст | Использование |
|--------|-----|-------|---------------|
| **Verified** | `bg-green-500/10` | `text-green-400` | Верифицированные пользователи |
| **Registered** | `bg-blue-500/10` | `text-blue-400` | Зарегистрированные пользователи |
| **Banned** | `bg-red-500/10` | `text-red-400` | Заблокированные пользователи |
| **Deleted** | `bg-gray-500/10` | `text-gray-400` | Удаленные пользователи |

#### Цвета для ролей

| Роль | Фон | Текст | Использование |
|------|-----|-------|---------------|
| **Admin** | `bg-purple-500/10` | `text-purple-400` | Администраторы |
| **Teacher** | `bg-orange-500/10` | `text-orange-400` | Преподаватели |
| **Student** | `bg-blue-500/10` | `text-blue-400` | Студенты |

### 1.2 Типографика

#### Шрифты

- **Основной шрифт:** Geist Sans (системный fallback: Arial, Helvetica, sans-serif)
- **Моноширинный:** Geist Mono (для кода, формул)

#### Размеры текста

| Размер | Tailwind класс | Размер (px) | Использование |
|--------|----------------|-------------|---------------|
| **xs** | `text-xs` | 12px | Мелкие подписи, метки |
| **sm** | `text-sm` | 14px | Вторичный текст, описания |
| **base** | `text-base` | 16px | Основной текст |
| **lg** | `text-lg` | 18px | Подзаголовки |
| **xl** | `text-xl` | 20px | Заголовки секций |
| **2xl** | `text-2xl` | 24px | Заголовки страниц |
| **4xl** | `text-4xl` | 36px | Главные заголовки |

#### Начертания

- **Regular:** `font-normal` (400)
- **Medium:** `font-medium` (500)
- **Semibold:** `font-semibold` (600)
- **Bold:** `font-bold` (700)

### 1.3 Скругления

| Размер | Tailwind класс | Радиус (px) | Использование |
|--------|----------------|-------------|---------------|
| **Маленькое** | `rounded-lg` | 8px | Кнопки, маленькие элементы |
| **Среднее** | `rounded-xl` | 12px | Инпуты, карточки |
| **Большое** | `rounded-2xl` | 16px | Модалки, большие карточки |
| **Круглое** | `rounded-full` | 9999px | Аватары, иконки |

### 1.4 Тени

| Тип | Tailwind класс | Использование |
|-----|----------------|---------------|
| **Малая** | `shadow-md` | Карточки, элементы |
| **Средняя** | `shadow-lg` | Модалки, выпадающие меню |
| **Большая** | `shadow-2xl` | Главные модалки, сайдбар |

### 1.5 Отступы

| Размер | Tailwind класс | Размер (px) | Использование |
|--------|----------------|-------------|---------------|
| **xs** | `p-1`, `gap-1` | 4px | Минимальные отступы |
| **sm** | `p-2`, `gap-2` | 8px | Маленькие отступы |
| **md** | `p-3`, `gap-3` | 12px | Средние отступы |
| **lg** | `p-4`, `gap-4` | 16px | Стандартные отступы |
| **xl** | `p-6`, `gap-6` | 24px | Большие отступы |
| **2xl** | `p-10`, `gap-10` | 40px | Очень большие отступы |

---

## 2. UI Компоненты

### 2.1 Button (Кнопка)

**Расположение:** `src/components/ui/Button.tsx`

#### Варианты (Variants)

##### Primary (по умолчанию)
```tsx
<Button variant="primary">Текст кнопки</Button>
```

**Стили:**
- **Фон:** `bg-white` (белый)
- **Текст:** `text-black font-semibold` (черный, жирный)
- **Граница:** `border border-white`
- **Тень:** `shadow-lg hover:shadow-xl`
- **При наведении:** 
  - Фон: `hover:bg-gray-100`
  - Масштаб: `hover:scale-[1.02]` (увеличение на 2%)
- **При нажатии:** `active:scale-[0.98]` (уменьшение на 2%)
- **Фокус:** `focus:ring-2 focus:ring-white/30`

**Использование:** Основные действия (сохранить, создать, подтвердить)

##### Secondary
```tsx
<Button variant="secondary">Текст кнопки</Button>
```

**Стили:**
- **Фон:** `bg-[#242424]`
- **Текст:** `text-white font-semibold`
- **Граница:** `border border-gray-700/50`
- **Тень:** `shadow-md hover:shadow-lg`
- **При наведении:** `hover:bg-[#2a2a2a] hover:scale-[1.02]`

**Использование:** Вторичные действия

##### Outline
```tsx
<Button variant="outline">Текст кнопки</Button>
```

**Стили:**
- **Фон:** `bg-transparent`
- **Текст:** `text-white`
- **Граница:** `border border-white`
- **При наведении:** `hover:bg-white hover:text-black`

**Использование:** Отмена, альтернативные действия

##### Danger
```tsx
<Button variant="danger">Удалить</Button>
```

**Стили:**
- **Фон:** `bg-red-600`
- **Текст:** `text-white font-semibold`
- **Граница:** `border border-red-600`
- **При наведении:** `hover:bg-red-700 hover:scale-[1.02]`

**Использование:** Удаление, опасные действия

##### Warning
```tsx
<Button variant="warning">Предупреждение</Button>
```

**Стили:**
- **Фон:** `bg-yellow-600`
- **Текст:** `text-white font-semibold`
- **Граница:** `border border-yellow-600`
- **При наведении:** `hover:bg-yellow-700 hover:scale-[1.02]`

**Использование:** Предупреждения

#### Размеры (Sizes)

| Размер | Tailwind класс | Padding | Размер текста | Использование |
|--------|----------------|---------|---------------|---------------|
| **sm** | `size="sm"` | `px-3 py-1.5` | `text-sm` | Маленькие кнопки |
| **md** | `size="md"` | `px-5 py-2` | `text-base` | Стандартные кнопки (по умолчанию) |
| **lg** | `size="lg"` | `px-8 py-3` | `text-lg` | Большие кнопки |

#### Состояния

##### Загрузка (Loading)
```tsx
<Button isLoading={true}>Загрузка...</Button>
```

**Особенности:**
- Показывается анимированный спиннер (вращающийся круг)
- Кнопка становится неактивной
- Текст заменяется на "Загрузка..." или остается с префиксом спиннера

**Спиннер:**
- Размер: `h-5 w-5`
- Анимация: `animate-spin`
- Цвет: `currentColor` (наследует цвет текста)

##### Отключенная (Disabled)
```tsx
<Button disabled>Недоступно</Button>
```

**Стили:**
- Прозрачность: `opacity-50`
- Курсор: `cursor-not-allowed`
- Все hover эффекты отключены

##### С подсказкой (Tooltip)
```tsx
<Button tooltip="Подсказка при наведении на disabled кнопку" disabled>
  Кнопка
</Button>
```

**Особенности:**
- Подсказка показывается только для disabled кнопок
- Стиль подсказки: темный фон `#1a1a1a`, желтая иконка предупреждения

#### Примеры использования

```tsx
// Основная кнопка
<Button type="submit" isLoading={isLoading}>
  Сохранить
</Button>

// Кнопка отмены
<Button variant="outline" onClick={onClose}>
  Отмена
</Button>

// Кнопка удаления
<Button variant="danger" onClick={handleDelete}>
  Удалить
</Button>

// Маленькая кнопка
<Button size="sm" variant="secondary">
  Действие
</Button>
```

### 2.2 Input (Поле ввода)

**Расположение:** `src/components/ui/Input.tsx`

#### Базовые стили

```tsx
<Input
  type="text"
  placeholder="Введите текст"
  value={value}
  onChange={(e) => setValue(e.target.value)}
/>
```

**Стили:**
- **Фон:** `#0b0b0b` (почти черный)
- **Текст:** `text-white`
- **Placeholder:** `placeholder-gray-400`
- **Отступы:** `px-5 py-4` (20px по горизонтали, 16px по вертикали)
- **Скругление:** `rounded-xl` (12px)
- **Граница:** `border border-gray-600`
- **При наведении:** `hover:border-gray-500`
- **При фокусе:** `focus:outline-none focus:border-white`
- **Переход:** `transition-all duration-300 ease-in-out`

#### Состояние ошибки

```tsx
<Input
  error="Обязательное поле"
  value={value}
  onChange={(e) => setValue(e.target.value)}
/>
```

**Стили при ошибке:**
- **Граница:** `border-red-400`
- **При фокусе:** `focus:border-red-400`
- **Текст ошибки:** Отображается под полем (`text-red-400 text-sm`)

#### Типы полей

##### Текстовое поле
```tsx
<Input type="text" placeholder="Введите текст" />
```

##### Поле пароля
```tsx
<Input type="password" placeholder="Введите пароль" />
```

##### Числовое поле
```tsx
<Input type="number" placeholder="Введите число" />
```

##### Email поле
```tsx
<Input type="email" placeholder="email@example.com" />
```

#### Примеры использования

```tsx
// Базовое поле
<Input
  id="name"
  name="name"
  value={formData.name}
  onChange={handleChange}
  placeholder="Введите имя"
  required
/>

// Поле с ошибкой
<Input
  value={formData.email}
  onChange={handleChange}
  error={errors.email}
  placeholder="Email"
/>

// Отключенное поле
<Input
  value={readonlyValue}
  disabled
  placeholder="Недоступно для редактирования"
/>
```

### 2.3 Select (Выпадающий список)

**Расположение:** `src/components/ui/Select.tsx`

#### Базовое использование

```tsx
<Select
  value={selectedValue}
  onChange={setSelectedValue}
  options={[
    { value: 'option1', label: 'Опция 1' },
    { value: 'option2', label: 'Опция 2' }
  ]}
  placeholder="Выберите опцию"
/>
```

#### Стили

**Trigger (кнопка открытия):**
- **Фон:** `bg-[#242424]`
- **Текст:** `text-white` (выбранное) / `text-gray-400` (placeholder)
- **Отступы:** `px-3 py-2`
- **Скругление:** `rounded-lg`
- **Граница:** `border-0` (без границы)
- **При фокусе:** `focus:ring-2 focus:ring-white/20`
- **При открытии:** `ring-2 ring-white/20`

**Dropdown (выпадающее меню):**
- **Фон:** `bg-[#242424]`
- **Граница:** `border border-gray-700`
- **Скругление:** `rounded-lg`
- **Тень:** `shadow-lg`
- **Максимальная высота:** `max-h-60` (240px)
- **Скролл:** Автоматический при переполнении

**Опции:**
- **Фон при hover:** `hover:bg-[#363636]`
- **Выбранная опция:** `bg-[#363636] text-white`
- **Невыбранная опция:** `text-gray-300`
- **Отступы:** `px-3 py-2`

#### Умное позиционирование

**Особенность:** Select автоматически определяет, открывать меню вверх или вниз в зависимости от доступного пространства.

**Логика:**
- Если места внизу меньше 240px, но вверху больше - открывается вверх
- Иначе открывается вниз

#### Примеры использования

```tsx
// Базовый select
<Select
  value={status}
  onChange={setStatus}
  options={[
    { value: 'active', label: 'Активный' },
    { value: 'inactive', label: 'Неактивный' }
  ]}
/>

// Select с placeholder
<Select
  value={role}
  onChange={setRole}
  options={roleOptions}
  placeholder="Выберите роль"
/>

// Отключенный select
<Select
  value={value}
  onChange={setValue}
  options={options}
  disabled
/>
```

### 2.4 Modal (Модальное окно)

**Базовый паттерн:** Все модальные окна следуют единому стилю.

#### Структура модального окна

```tsx
<div className="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50 p-4">
  <div className="bg-[#151515] rounded-2xl w-full max-w-md border border-gray-800">
    {/* Заголовок */}
    <div className="flex items-center justify-between p-6 pb-4">
      <h3 className="text-lg font-semibold text-white">Заголовок</h3>
      <button onClick={onClose}>
        <Icons.X className="h-5 w-5 text-gray-400" />
      </button>
    </div>
    
    {/* Содержимое */}
    <div className="px-6 pb-6">
      {/* Форма или контент */}
    </div>
  </div>
</div>
```

#### Стили

**Overlay (фон):**
- **Фон:** `bg-black/50` (черный с прозрачностью 50%)
- **Размытие:** `backdrop-blur-sm`
- **Позиционирование:** `fixed inset-0`
- **Z-index:** `z-50`

**Контейнер модалки:**
- **Фон:** `bg-[#151515]`
- **Скругление:** `rounded-2xl` (16px)
- **Граница:** `border border-gray-800`
- **Максимальная ширина:** `max-w-md` (448px) или `max-w-lg` (512px) для больших модалок
- **Тень:** Встроенная через фон

**Заголовок:**
- **Отступы:** `p-6 pb-4`
- **Размер текста:** `text-lg font-semibold`
- **Цвет:** `text-white`

**Содержимое:**
- **Отступы:** `px-6 pb-6`
- **Скролл:** Автоматический при переполнении (класс `modal-scroll`)

#### Типы модалок

##### CreateModal (Создание)
- Заголовок: "Создать [сущность]"
- Форма с полями
- Кнопки: "Отмена" (outline) и "Создать" (primary)

##### EditModal (Редактирование)
- Заголовок: "Редактировать [сущность]"
- Форма с предзаполненными данными
- Кнопки: "Отмена" (outline) и "Сохранить" (primary)

##### ViewModal (Просмотр)
- Заголовок: "Информация о [сущности]"
- Только для чтения
- Кнопка: "Закрыть" (primary)

##### ConfirmDialog (Подтверждение)
- С иконкой (danger/warning/info)
- Сообщение
- Кнопки: "Отмена" (outline) и "Подтвердить" (primary/danger/warning)

#### Примеры модалок

##### CreateSubscriptionModal
```tsx
<CreateSubscriptionModal
  isOpen={showCreateModal}
  onClose={() => setShowCreateModal(false)}
  onSubmit={handleCreateSubscription}
/>
```

**Особенности:**
- Переключатель языка (RU/KG)
- Локализованные поля (название, описание, возможности)
- Динамические поля для features (ключ-значение)
- Валидация формы

##### EditSubscriptionModal
```tsx
<EditSubscriptionModal
  isOpen={showEditModal}
  onClose={() => setShowEditModal(false)}
  onSubmit={handleUpdateSubscription}
  subscription={selectedSubscription}
/>
```

**Особенности:**
- Предзаполнение данных
- Парсинг JSON для локализованных полей
- Сохранение изменений

##### ConfirmDialog
```tsx
<ConfirmDialog
  isOpen={isDeleteDialogOpen}
  onClose={() => setIsDeleteDialogOpen(false)}
  onConfirm={handleDelete}
  title="Удалить подписку?"
  message="Это действие нельзя отменить."
  variant="danger"
  isLoading={isDeleting}
/>
```

**Варианты:**
- `danger` - удаление (красная иконка)
- `warning` - предупреждение (желтая иконка)
- `info` - информация (белая иконка)
- `refund` - возврат (белая иконка возврата)

### 2.5 Toast (Уведомления)

**Расположение:** `src/components/ui/Toast.tsx`

#### Использование

```tsx
const { showToast } = useToast();

showToast({
  type: 'success',
  title: 'Успешно!',
  message: 'Подписка создана'
});
```

#### Типы уведомлений

| Тип | Иконка | Использование |
|-----|--------|---------------|
| **success** | CheckCircle (белая) | Успешные операции |
| **error** | AlertCircle (белая) | Ошибки |
| **warning** | AlertCircle (белая) | Предупреждения |
| **info** | Info (белая) | Информация |

#### Стили

**Контейнер:**
- **Позиция:** `fixed top-4 right-4 z-[9999]`
- **Максимальная ширина:** `max-w-md`
- **Отступы между уведомлениями:** `gap-3`

**Уведомление:**
- **Фон:** `bg-[#151515] border border-gray-800`
- **Скругление:** `rounded-2xl`
- **Тень:** `shadow-lg`
- **Размытие:** `backdrop-blur-sm`
- **Анимация:** `animate-slide-in-right` (выезд справа)

**Иконка:**
- **Фон:** `bg-[#242424]`
- **Размер:** `h-5 w-5`
- **Цвет:** `text-white`

**Текст:**
- **Заголовок:** `text-sm font-semibold text-white`
- **Сообщение:** `text-sm text-gray-400`

**Длительность:**
- По умолчанию: 5000ms (5 секунд)
- Можно задать кастомную: `duration: 10000`

#### Дополнительные возможности

##### С кнопкой действия
```tsx
showToast({
  type: 'success',
  title: 'Изменения сохранены',
  onAction: () => handleUndo(),
  actionLabel: 'Отменить'
});
```

##### С кастомным контентом
```tsx
showToast({
  type: 'info',
  title: 'Обновление',
  customContent: <div>Кастомный контент</div>
});
```

#### Примеры использования

```tsx
// Успех
showToast({
  type: 'success',
  title: 'Подписка создана',
  message: 'Подписка успешно добавлена в систему'
});

// Ошибка
showToast({
  type: 'error',
  title: 'Ошибка',
  message: 'Не удалось создать подписку'
});

// С действием
showToast({
  type: 'success',
  title: 'Платеж возвращен',
  message: 'Средства будут возвращены в течение 3-5 дней',
  onAction: () => handleViewDetails(),
  actionLabel: 'Подробнее'
});
```

### 2.6 Tooltip (Подсказки)

**Расположение:** `src/components/ui/Tooltip.tsx`

#### Использование

```tsx
<Tooltip text="Подсказка при наведении">
  <button>Кнопка</button>
</Tooltip>
```

#### Стили

**Подсказка:**
- **Фон:** `#151515`
- **Граница:** `1px solid #404040`
- **Тень:** `0 2px 8px rgba(0, 0, 0, 0.3)`
- **Текст:** `text-white text-xs`
- **Скругление:** `rounded-md`
- **Z-index:** `z-[9999]`

**Треугольный указатель:**
- Темно-серый с границей
- Автоматическое позиционирование

#### Позиции

| Позиция | Класс | Описание |
|---------|-------|----------|
| **top** | `position="top"` | Сверху |
| **bottom** | `position="bottom"` | Снизу (по умолчанию) |
| **left** | `position="left"` | Слева |
| **right** | `position="right"` | Справа |

#### Кастомные подсказки (useCustomTooltips)

**Хук:** `useCustomTooltips()`

**Использование:**
```tsx
import { useCustomTooltips } from '@/hooks/useCustomTooltips';

function Component() {
  useCustomTooltips();
  
  return (
    <button data-tooltip="Подсказка">Кнопка</button>
  );
}
```

**Особенности:**
- Работает через атрибут `data-tooltip`
- Автоматическое позиционирование
- Поддержка локализации
- Единый стиль для всех подсказок

### 2.7 Table (Таблица)

**Паттерн:** Таблицы реализованы через компоненты страниц, но следуют единому стилю.

#### Стили таблицы

**Контейнер:**
- **Фон:** `bg-[#151515]`
- **Скругление:** `rounded-2xl`
- **Переполнение:** `overflow-x-auto`

**Заголовки (th):**
- **Фон:** `bg-[#242424]`
- **Текст:** `text-white font-semibold`
- **Отступы:** `px-4 py-3`
- **Границы:** `border-b border-gray-700`

**Ячейки (td):**
- **Фон:** `bg-[#151515]`
- **Текст:** `text-gray-300`
- **Отступы:** `px-4 py-3`
- **Границы:** `border-b border-gray-700`

**Строки:**
- **При наведении:** `hover:bg-[#1a1a1a]`
- **Переход:** `transition-colors`

#### Пример таблицы

```tsx
<div className="bg-[#151515] rounded-2xl overflow-x-auto">
  <table className="w-full">
    <thead>
      <tr className="bg-[#242424]">
        <th className="px-4 py-3 text-left text-white font-semibold">Название</th>
        <th className="px-4 py-3 text-left text-white font-semibold">Цена</th>
        <th className="px-4 py-3 text-left text-white font-semibold">Действия</th>
      </tr>
    </thead>
    <tbody>
      {items.map((item) => (
        <tr key={item.id} className="border-b border-gray-700 hover:bg-[#1a1a1a]">
          <td className="px-4 py-3 text-gray-300">{item.name}</td>
          <td className="px-4 py-3 text-gray-300">{item.price}</td>
          <td className="px-4 py-3">
            <button>Действие</button>
          </td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
```

### 2.8 Card (Карточка)

**Паттерн:** Карточки используются для отображения данных в сетке.

#### Стили карточки

```tsx
<div className="bg-[#151515] rounded-2xl p-6">
  {/* Содержимое */}
</div>
```

**Базовые стили:**
- **Фон:** `bg-[#151515]`
- **Скругление:** `rounded-2xl`
- **Отступы:** `p-6` (24px)
- **Тень:** Встроенная через фон

#### Примеры карточек

##### Статистическая карточка
```tsx
<div className="bg-[#151515] rounded-2xl p-6">
  <div className="flex items-center justify-between mb-4">
    <div className="p-2 bg-blue-500/10 rounded-lg">
      <Icons.Users className="h-6 w-6 text-blue-400" />
    </div>
    <span className="text-xs text-green-400 bg-green-400/10 px-2 py-1 rounded-full">
      +12%
    </span>
  </div>
  <h3 className="text-sm font-medium text-gray-400 mb-1">Пользователи</h3>
  <p className="text-2xl font-bold text-white">1,234</p>
  <p className="text-xs text-gray-500 mt-1">всего зарегистрировано</p>
</div>
```

##### Карточка курса
```tsx
<div className="bg-[#151515] rounded-2xl p-6 hover:bg-[#1a1a1a] transition-colors cursor-pointer">
  <h3 className="text-lg font-semibold text-white mb-2">Название курса</h3>
  <p className="text-sm text-gray-400 mb-4">Описание курса</p>
  <div className="flex items-center justify-between">
    <span className="text-sm text-gray-500">24 урока</span>
    <Button size="sm">Открыть</Button>
  </div>
</div>
```

---

## 3. Layout (Макет)

### 3.1 DashboardLayout

**Расположение:** `src/components/dashboard/DashboardLayout.tsx`

#### Структура

```
┌─────────────────────────────────────┐
│           Header (fixed)            │
├──────────┬──────────────────────────┤
│          │                          │
│ Sidebar  │    Main Content          │
│ (fixed)  │    (scrollable)          │
│          │                          │
│          │                          │
└──────────┴──────────────────────────┘
```

#### Стили

**Обертка:**
- **Фон:** `bg-[#0b0b0b]`
- **Высота:** `h-screen`
- **Flex:** `flex flex-col`
- **Переполнение:** `overflow-hidden`

**Main Content:**
- **Отступ сверху:** `pt-24` (для header)
- **Отступ слева (desktop):** `lg:pl-72` (для sidebar)
- **Отступ справа:** `pr-4`
- **Flex:** `flex-1 min-h-0`
- **Переполнение:** `overflow-hidden`

**Внутренний контейнер:**
- **Отступы:** `p-4 lg:p-6`
- **Высота:** `h-full`
- **Скролл:** Автоматический

### 3.2 Header (Шапка)

**Расположение:** `src/components/dashboard/Header.tsx`

#### Структура

```
┌─────────────────────────────────────────────────────┐
│ [☰] Logo  Title    [🔍] [🌐] [🔔] [👤] Name  [▼]  │
└─────────────────────────────────────────────────────┘
```

#### Стили

**Контейнер:**
- **Позиция:** `fixed top-4 left-4 right-4`
- **Фон:** `bg-[#151515]`
- **Скругление:** `rounded-2xl`
- **Отступы:** `px-4 lg:px-6`
- **Высота:** `h-16`
- **Z-index:** `z-50`
- **Тень:** `shadow-2xl`
- **Flex:** `flex items-center justify-between`

**Левая часть:**
- Кнопка меню (мобильная)
- Логотип (белый квадрат с буквой "B")
- Название "Bilimpoz Admin"

**Правая часть:**
- Поиск (скрыт на мобильных)
- Переключатель языка
- Уведомления (с красной точкой)
- Профиль (аватар + имя + выпадающее меню)

#### Элементы

##### Поиск
```tsx
<div className="hidden md:flex items-center relative">
  <Icons.Search className="absolute left-4 h-4 w-4 text-gray-400" />
  <input
    type="text"
    placeholder="Поиск..."
    className="pl-12 pr-4 py-3 bg-[#242424] border-0 rounded-xl text-sm text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-white/20 transition-all w-64"
  />
</div>
```

##### Переключатель языка
- Компактный дизайн
- Показывает текущий язык (RU/KG)
- Выпадающее меню с флагами

##### Профиль
- Аватар (фото или иконка)
- Имя пользователя
- Выпадающее меню с кнопкой "Выйти"

### 3.3 Sidebar (Боковая панель)

**Расположение:** `src/components/dashboard/Sidebar.tsx`

#### Структура

```
┌─────────────────┐
│   Logo  Title   │ (мобильная версия)
├─────────────────┤
│ 📊 Дашборд      │
│ 📚 Курсы        │
│ 👥 Пользователи │
│ ✉️  Рассылки    │
│ 💳 Подписки     │
│ 📝 Тесты        │
│ ❓ Вопросы      │
│ ⚙️  Настройки   │
│                 │
│                 │
│ ┌─────────────┐ │
│ │ 👤 Профиль  │ │
│ │ [Выйти]     │ │
│ └─────────────┘ │
└─────────────────┘
```

#### Стили

**Контейнер:**
- **Позиция:** `fixed top-24 left-4`
- **Высота:** `h-[calc(100vh-7rem)]`
- **Ширина:** `w-64` (256px)
- **Фон:** `bg-[#151515]`
- **Скругление:** `rounded-2xl`
- **Тень:** `shadow-2xl`
- **Z-index:** `z-40`
- **Переход:** `transform transition-transform duration-300`

**Мобильная версия:**
- Скрыта по умолчанию (`-translate-x-full`)
- Показывается при открытии (`translate-x-0`)
- Overlay при открытии (темный фон с прозрачностью)

**Навигация:**
- Отступы: `p-4`
- Отступы между элементами: `space-y-2`

**Пункты меню:**
- **Активный:** `bg-white text-black`
- **Неактивный:** `text-gray-300 hover:bg-[#242424] hover:text-white`
- **Отступы:** `px-3 py-2.5`
- **Скругление:** `rounded-lg`
- **Переход:** `transition-colors`

**Нижняя часть (профиль):**
- **Позиция:** `absolute bottom-0`
- **Фон:** `bg-[#242424]`
- **Граница:** `border border-gray-700/50`
- **Скругление:** `rounded-2xl`
- **Отступы:** `p-4`

#### Пункты меню

| Иконка | Название | Путь |
|--------|----------|------|
| 📊 | Дашборд | `/dashboard` |
| 📚 | Курсы | `/dashboard/courses` |
| 👥 | Пользователи | `/dashboard/users` |
| ✉️ | Рассылки | `/dashboard/newsletters` |
| 💳 | Подписки | `/dashboard/subscriptions` |
| 📝 | Тесты | `/dashboard/trial-tests` |
| ❓ | Вопросы | `/dashboard/questions` |
| ⚙️ | Настройки | `/dashboard/settings` |

---

## 4. Страницы и функциональность

### 4.1 Dashboard (Главная страница)

**Расположение:** `src/app/dashboard/page.tsx`

#### Структура

1. **Заголовок страницы**
   - Название: "Дашборд"
   - Описание: "Обзор основных метрик и аналитики"

2. **Карточки с метриками** (4 карточки в ряд)
   - Пользователи
   - Активные курсы
   - Доходы
   - Тесты

3. **Быстрые действия** (левая колонка)
   - Кнопки быстрых действий

4. **Недавняя активность** (правая колонка)
   - Список последних действий

#### Карточки метрик

**Структура карточки:**
```tsx
<div className="bg-[#151515] rounded-2xl p-6">
  <div className="flex items-center justify-between mb-4">
    <div className="p-2 bg-{color}-500/10 rounded-lg">
      <Icon className="h-6 w-6 text-{color}-400" />
    </div>
    <span className="text-xs text-green-400 bg-green-400/10 px-2 py-1 rounded-full">
      +12%
    </span>
  </div>
  <h3 className="text-sm font-medium text-gray-400 mb-1">Название</h3>
  <p className="text-2xl font-bold text-white">Значение</p>
  <p className="text-xs text-gray-500 mt-1">Описание</p>
</div>
```

### 4.2 Subscriptions (Подписки)

**Расположение:** `src/app/dashboard/subscriptions/page.tsx`

#### Структура

1. **Вкладки**
   - "Подписки" (subscriptions)
   - "Платежи" (payments)

2. **Раздел "Подписки"**
   - Статистика (круговая диаграмма, график роста)
   - Фильтры (статус, дата)
   - Таблица подписок
   - Кнопка "Создать подписку"

3. **Раздел "Платежи"**
   - Статистика платежей
   - Фильтры (статус, сумма, подписка, поиск, дата)
   - Таблица платежей
   - Экспорт в Excel

#### Функциональность

##### Создание подписки
- Модальное окно с формой
- Локализация (RU/KG)
- Динамические поля features (ключ-значение)
- Валидация

##### Редактирование подписки
- Модальное окно с предзаполненными данными
- Парсинг JSON для локализованных полей
- Сохранение изменений

##### Удаление подписки
- Диалог подтверждения
- Удаление из базы данных

##### Популярная подписка
- Звездочка для выбора популярной подписки
- Сохранение в таблицу `settings` (ключ: `POPULAR_SUBSCRIPTION`)
- Только одна подписка может быть популярной

##### Фильтрация
- По статусу (Все / Активные / Неактивные)
- По дате (период)
- Активная подписка = `subscribers_count > 0`

##### Графики
- Круговая диаграмма (распределение по подпискам)
- Линейный график (динамика роста)

### 4.3 Users (Пользователи)

**Расположение:** `src/app/dashboard/users/page.tsx`

#### Структура

1. **Заголовок и фильтры**
   - Название страницы
   - Фильтры (роль, статус, язык, поиск)

2. **Таблица пользователей**
   - Колонки: Аватар, Имя, Логин, Роль, Статус, Язык, Дата регистрации, Действия
   - Сортировка
   - Пагинация

3. **Модальные окна**
   - Просмотр деталей пользователя
   - Редактирование пользователя
   - Блокировка/разблокировка
   - Просмотр жалоб

#### Функциональность

##### Просмотр пользователя
- Детальная информация
- Статистика (для студентов и преподавателей)
- Активные подписки
- Жалобы

##### Редактирование пользователя
- Изменение имени, роли, статуса
- Привязка Telegram

##### Блокировка пользователя
- Диалог подтверждения
- Изменение статуса на "banned"

##### Жалобы
- Модальное окно со списком жалоб
- Ответ на жалобу
- Изменение статуса жалобы

### 4.4 Courses (Курсы)

**Расположение:** `src/app/dashboard/courses/page.tsx`

#### Структура

1. **Список курсов** (карточки)
   - Название курса
   - Описание
   - Количество групп
   - Действия (редактировать, удалить)

2. **Модальные окна**
   - Создание курса
   - Редактирование курса
   - Просмотр информации

3. **Вложенная структура**
   - Курс → Группы → Уроки
   - Навигация по уровням

#### Функциональность

##### Создание курса
- Название
- Описание
- Цвет (выбор из палитры или кастомный)
- Изображение

##### Группы уроков
- Создание группы
- Выбор цвета группы
- Сортировка групп (drag & drop)

##### Уроки
- Создание урока
- Редактор контента (Markdown)
- Видео
- Тесты

---

## 5. Специальные компоненты

### 5.1 LanguageSwitcher (Переключатель языка)

**Расположение:** `src/components/ui/LanguageSwitcher.tsx`

#### Дизайн

**Кнопка:**
- Компактная (только код языка: RU/KG)
- Иконка стрелки вниз
- Hover эффект

**Выпадающее меню:**
- Флаги стран
- Выбранный язык: белый фон, черный текст
- Невыбранный: серый текст, hover эффект

#### Функциональность

- Сохранение языка в cookie
- Обновление страницы при смене языка
- Поддержка локализации всех текстов

### 5.2 DateFilter (Фильтр по дате)

**Расположение:** `src/components/dashboard/DateFilter.tsx`

#### Варианты фильтров

- Сегодня
- Вчера
- Последние 7 дней
- Последние 30 дней
- Этот месяц
- Прошлый месяц
- Произвольный период (от-до)

#### Интерфейс

- Выбор периода из списка
- Календарь для произвольного периода
- Выбор времени (опционально)

### 5.3 Charts (Графики)

#### PieChart (Круговая диаграмма)

**Расположение:** `src/components/ui/PieChart.tsx`

**Особенности:**
- Анимированная отрисовка
- Легенда с процентами
- Hover эффекты
- Кастомные цвета

#### LineChart (Линейный график)

**Расположение:** `src/components/ui/LineChart.tsx`

**Особенности:**
- Плавные линии
- Точки данных
- Подписи осей
- Адаптивный размер

### 5.4 Markdown Editor (Редактор Markdown)

**Библиотека:** `@uiw/react-md-editor`

#### Стилизация

Все стили кастомизированы под темную тему:

**Панель инструментов:**
- Фон: `#151515`
- Кнопки: серые, при hover - белые
- Активная кнопка: белый фон, черный текст

**Область редактирования:**
- Фон: `#0b0b0b`
- Текст: белый
- Моноширинный шрифт

**Область предпросмотра:**
- Фон: `#0b0b0b`
- Markdown стили под темную тему
- Поддержка LaTeX формул (KaTeX)

---

## 6. Адаптивность

### 6.1 Брейкпоинты

| Размер | Tailwind класс | Ширина | Описание |
|--------|----------------|--------|----------|
| **sm** | `sm:` | 640px | Планшеты (портретная) |
| **md** | `md:` | 768px | Планшеты (альбомная) |
| **lg** | `lg:` | 1024px | Ноутбуки |
| **xl** | `xl:` | 1280px | Десктопы |
| **2xl** | `2xl:` | 1536px | Большие экраны |

### 6.2 Мобильная версия

#### Header
- Кнопка меню показывается
- Поиск скрыт
- Компактные элементы

#### Sidebar
- Скрыт по умолчанию
- Открывается по клику на меню
- Overlay при открытии
- Полная ширина на мобильных

#### Контент
- Отступы уменьшены (`p-4` вместо `p-6`)
- Карточки в одну колонку
- Таблицы с горизонтальным скроллом

### 6.3 Планшетная версия

- Сайдбар может быть скрыт
- Карточки в 2 колонки
- Поиск показывается

### 6.4 Десктопная версия

- Сайдбар всегда виден
- Полная функциональность
- Оптимальные отступы

---

## 7. Анимации и переходы

### 7.1 Переходы

#### Кнопки
- **Длительность:** `duration-200` (200ms)
- **Эффект:** Масштабирование при hover/active
- **Переход:** `transition-all`

#### Инпуты
- **Длительность:** `duration-300` (300ms)
- **Эффект:** Изменение границы
- **Переход:** `transition-all ease-in-out`

#### Модалки
- **Длительность:** `duration-300` (300ms)
- **Эффект:** Появление с размытием фона

### 7.2 Анимации

#### Slide-in-right (Toast)
```css
@keyframes slide-in-right {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}
```

#### Shimmer (Skeleton)
```css
@keyframes shimmer {
  0% {
    background-position: -200% 0;
  }
  100% {
    background-position: 200% 0;
  }
}
```

#### Spin (Загрузка)
```css
@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
```

### 7.3 Hover эффекты

#### Кнопки
- Масштабирование: `scale-[1.02]`
- Изменение фона
- Изменение тени

#### Карточки
- Изменение фона: `hover:bg-[#1a1a1a]`
- Плавный переход

#### Строки таблицы
- Изменение фона: `hover:bg-[#1a1a1a]`
- Плавный переход

---

## 8. Скроллбары

### 8.1 Основной скроллбар

**Стили:**
- **Ширина:** 8px
- **Трек:** `#1a1a1a`
- **Ползунок:** `#404040`
- **Ползунок при hover:** `#606060`
- **Скругление:** 4px

### 8.2 Скроллбар модалок

**Стили:**
- **Ширина:** 3px
- **Трек:** прозрачный
- **Ползунок:** `#4a5568`
- **Ползунок при hover:** `#6b7280`
- **Скругление:** 2px

### 8.3 Скроллбар Markdown редактора

**Стили:**
- **Ширина:** 8px
- **Трек:** `#0b0b0b`
- **Ползунок:** `#404040`
- **Ползунок при hover:** `#606060`

---

## 9. Формы и валидация

### 9.1 Валидация полей

#### Обязательные поля
- Отмечены звездочкой (*)
- Проверка на пустоту
- Сообщение об ошибке под полем

#### Валидация типов
- Email: проверка формата
- Числа: проверка диапазона
- Даты: проверка формата

#### Сообщения об ошибках
- Цвет: `text-red-400`
- Размер: `text-sm`
- Позиция: под полем (`mt-1`)

### 9.2 Состояния формы

#### Обычное состояние
- Белая граница при фокусе
- Серый placeholder

#### Состояние ошибки
- Красная граница
- Сообщение об ошибке

#### Состояние загрузки
- Кнопка отправки показывает спиннер
- Поля отключены
- Кнопка неактивна

---

## 10. Иконки

### 10.1 Библиотека иконок

**Расположение:** `src/components/ui/Icons.tsx`

Все иконки - SVG компоненты React.

### 10.2 Основные иконки

| Иконка | Компонент | Использование |
|--------|-----------|---------------|
| 📊 | `BarChart3` | Дашборд, статистика |
| 📚 | `BookOpen` | Курсы |
| 👥 | `UserGroup` | Пользователи |
| ✉️ | `Mail` | Рассылки |
| 💳 | `CreditCard` | Подписки, платежи |
| 📝 | `FileText` | Тесты, документы |
| ❓ | `HelpCircle` | Вопросы, помощь |
| ⚙️ | `Settings` | Настройки |
| 🔍 | `Search` | Поиск |
| 🔔 | `Bell` | Уведомления |
| 👤 | `User` | Профиль |
| ✏️ | `Edit` | Редактирование |
| 🗑️ | `Trash` | Удаление |
| ✅ | `CheckCircle` | Успех |
| ⚠️ | `AlertTriangle` | Предупреждение |
| ❌ | `X` | Закрыть |
| ⬅️ | `ArrowLeft` | Назад |
| ⬇️ | `ChevronDown` | Раскрыть |
| 🌐 | Язык | Переключатель языка |

### 10.3 Размеры иконок

| Размер | Tailwind класс | Размер (px) | Использование |
|--------|----------------|-------------|---------------|
| **xs** | `h-3 w-3` | 12px | Очень маленькие иконки |
| **sm** | `h-4 w-4` | 16px | Маленькие иконки |
| **md** | `h-5 w-5` | 20px | Стандартные иконки |
| **lg** | `h-6 w-6` | 24px | Большие иконки |
| **xl** | `h-8 w-8` | 32px | Очень большие иконки |

---

## 11. Локализация

### 11.1 Поддерживаемые языки

- **Русский (RU)** - основной язык
- **Кыргызский (KG)** - дополнительный язык

### 11.2 Хранение переводов

**Расположение:** `src/locales/ru.json`, `src/locales/kg.json`

### 11.3 Использование

```tsx
import { useTranslation } from '@/hooks/useTranslation';

function Component() {
  const { t } = useTranslation();
  
  return <h1>{t('header.title')}</h1>;
}
```

### 11.4 Локализованные данные

Некоторые данные хранятся в JSON формате в базе данных:
```json
{
  "ru": "Русский текст",
  "ky": "Кыргызский текст"
}
```

**Извлечение:**
```tsx
const extractLocalizedText = (text: string): string => {
  try {
    const parsed = JSON.parse(text);
    return parsed[currentLang] || parsed.ru || text;
  } catch {
    return text;
  }
};
```

---

## 12. Состояния загрузки

### 12.1 Skeleton (Скелетон)

**Расположение:** `src/components/ui/Skeleton.tsx`

**Использование:**
```tsx
<Skeleton className="h-4 w-full" />
```

**Стили:**
- Анимация shimmer
- Серый фон
- Плавное появление

### 12.2 Spinner (Спиннер)

**Использование:**
- В кнопках при загрузке
- В модалках при сохранении
- На страницах при загрузке данных

**Стили:**
- Вращающийся круг
- Цвет наследуется от текста
- Размер: `h-5 w-5`

---

## 13. Заключение

Дизайн-система Bilimpoz Admin обеспечивает:

✅ **Единообразие:** Все компоненты следуют единому стилю  
✅ **Адаптивность:** Работает на всех устройствах  
✅ **Доступность:** Правильная семантика и навигация  
✅ **Производительность:** Оптимизированные анимации и переходы  
✅ **Расширяемость:** Легко добавлять новые компоненты  
✅ **Локализация:** Поддержка нескольких языков  

Все компоненты документированы, переиспользуемы и следуют лучшим практикам React и Next.js.

---

**Версия документа:** 1.0  
**Дата последнего обновления:** 2024  
**Автор:** Bilimpoz Admin Development Team

