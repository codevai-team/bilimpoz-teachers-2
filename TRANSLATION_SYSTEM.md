# Система перевода страниц (i18n)

## Обзор

Проект использует библиотеку **i18next** в связке с **react-i18next** для обеспечения многоязычности приложения. Система поддерживает два языка:
- **Русский (ru)** - язык по умолчанию
- **Кыргызский (ky)** - дополнительный язык

## Архитектура системы

### Компоненты системы

1. **Конфигурация i18n** (`src/lib/i18n.ts`)
2. **Провайдер i18n** (`src/components/providers/I18nProvider.tsx`)
3. **Хук перевода** (`src/hooks/useTranslation.ts`)
4. **Переключатель языка** (`src/components/ui/LanguageSwitcher.tsx`)
5. **Middleware** (`src/lib/middleware.ts`)
6. **Файлы переводов** (`src/locales/ru.json`, `src/locales/ky.json`)

---

## 1. Конфигурация i18n (`src/lib/i18n.ts`)

### Описание

Основной файл конфигурации i18next. Здесь происходит инициализация библиотеки, загрузка переводов и настройка детектора языка.

### Код

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

// Импорт переводов
import ruTranslations from '../locales/ru.json';
import kyTranslations from '../locales/ky.json';

const resources = {
  ru: {
    translation: ruTranslations,
  },
  ky: {
    translation: kyTranslations,
  },
};

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources,
    fallbackLng: 'ru',
    debug: false,
    
    interpolation: {
      escapeValue: false, // React уже экранирует значения
    },
    
    detection: {
      order: ['cookie', 'localStorage', 'navigator', 'htmlTag'],
      caches: ['cookie', 'localStorage'],
      lookupCookie: 'lang',
      lookupLocalStorage: 'i18nextLng',
      cookieMinutes: 525600, // 1 год в минутах
    },
  });

export default i18n;
```

### Параметры конфигурации

#### `resources`
- **Тип**: `Object`
- **Описание**: Объект, содержащий переводы для каждого языка
- **Структура**:
  ```typescript
  {
    ru: { translation: ruTranslations },
    ky: { translation: kyTranslations }
  }
  ```

#### `fallbackLng: 'ru'`
- **Тип**: `string`
- **Описание**: Язык, который используется, если текущий язык не найден или отсутствует перевод для ключа

#### `interpolation.escapeValue: false`
- **Тип**: `boolean`
- **Описание**: Отключает автоматическое экранирование значений, так как React уже делает это через JSX

#### `detection`
- **Тип**: `Object`
- **Описание**: Настройки автоматического определения языка

  - **`order`**: Порядок проверки источников языка
    1. `cookie` - проверяет cookie с именем `lang`
    2. `localStorage` - проверяет `localStorage` с ключом `i18nextLng`
    3. `navigator` - определяет язык из настроек браузера
    4. `htmlTag` - читает атрибут `lang` из HTML-тега
  
  - **`caches`**: Куда сохранять определенный язык
    - `cookie` - сохраняет в cookie
    - `localStorage` - сохраняет в localStorage
  
  - **`lookupCookie: 'lang'`**: Имя cookie для хранения языка
  - **`lookupLocalStorage: 'i18nextLng'`**: Ключ localStorage для хранения языка
  - **`cookieMinutes: 525600`**: Время жизни cookie в минутах (1 год = 365 * 24 * 60)

### Функции

#### `.use(LanguageDetector)`
- **Описание**: Подключает плагин автоматического определения языка из различных источников (cookie, localStorage, браузер)

#### `.use(initReactI18next)`
- **Описание**: Инициализирует интеграцию i18next с React, добавляя поддержку хука `useTranslation`

#### `.init(config)`
- **Описание**: Инициализирует i18next с переданной конфигурацией
- **Параметры**: `config` - объект конфигурации (resources, fallbackLng, detection и т.д.)

---

## 2. Провайдер i18n (`src/components/providers/I18nProvider.tsx`)

### Описание

React-компонент-обертка, который обеспечивает инициализацию i18n на клиентской стороне. Импортирует конфигурацию i18n и делает её доступной для всех дочерних компонентов.

### Код

```typescript
'use client';

import { useEffect } from 'react';
import '@/lib/i18n';

interface I18nProviderProps {
  children: React.ReactNode;
}

export default function I18nProvider({ children }: I18nProviderProps) {
  useEffect(() => {
    // i18n уже инициализирован через импорт
  }, []);

  return <>{children}</>;
}
```

### Особенности

- **`'use client'`**: Указывает Next.js, что компонент должен выполняться на клиенте
- **Импорт `@/lib/i18n`**: При импорте выполняется инициализация i18next
- **Пустой `useEffect`**: Компонент готов для добавления дополнительной логики при необходимости

### Использование в layout.tsx

```typescript
export default function RootLayout({ children }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body>
        <I18nProvider>
          {children}
        </I18nProvider>
      </body>
    </html>
  );
}
```

---

## 3. Хук перевода (`src/hooks/useTranslation.ts`)

### Описание

Кастомный хук-обертка над стандартным `useTranslation` из `react-i18next`. Предоставляет удобные функции для работы с переводами и языками.

### Код

```typescript
import { useTranslation as useI18nTranslation } from 'react-i18next';

export const useTranslation = () => {
  const { t, i18n } = useI18nTranslation();
  
  const changeLanguage = (lng: string) => {
    i18n.changeLanguage(lng);
  };
  
  const getCurrentLanguage = () => {
    return i18n.language;
  };
  
  const isRussian = () => {
    return i18n.language === 'ru';
  };
  
  const isKyrgyz = () => {
    return i18n.language === 'ky';
  };
  
  return {
    t,
    changeLanguage,
    getCurrentLanguage,
    isRussian,
    isKyrgyz,
    language: i18n.language,
  };
};
```

### Возвращаемые функции и свойства

#### `t(key: string, options?: Object)`
- **Тип**: `Function`
- **Описание**: Функция для получения переведенного текста по ключу
- **Параметры**:
  - `key`: Строка с путем к ключу перевода (например, `'users.title'`)
  - `options`: Опциональный объект с параметрами (для интерполяции переменных)
- **Возвращает**: `string` - переведенный текст
- **Примеры**:
  ```typescript
  t('users.title') // "Пользователи" или "Колдонуучулар"
  t('courses.groupsTitle', { courseName: 'Математика' }) // "Группы - Математика"
  ```

#### `changeLanguage(lng: string)`
- **Тип**: `Function`
- **Описание**: Меняет текущий язык приложения
- **Параметры**:
  - `lng`: Код языка (`'ru'` или `'ky'`)
- **Пример**:
  ```typescript
  changeLanguage('ky'); // Переключает на кыргызский
  ```

#### `getCurrentLanguage()`
- **Тип**: `Function`
- **Описание**: Возвращает текущий язык приложения
- **Возвращает**: `string` - код языка (`'ru'` или `'ky'`)
- **Пример**:
  ```typescript
  const lang = getCurrentLanguage(); // 'ru' или 'ky'
  ```

#### `isRussian()`
- **Тип**: `Function`
- **Описание**: Проверяет, является ли текущий язык русским
- **Возвращает**: `boolean` - `true` если язык русский, иначе `false`
- **Пример**:
  ```typescript
  if (isRussian()) {
    // Логика для русского языка
  }
  ```

#### `isKyrgyz()`
- **Тип**: `Function`
- **Описание**: Проверяет, является ли текущий язык кыргызским
- **Возвращает**: `boolean` - `true` если язык кыргызский, иначе `false`
- **Пример**:
  ```typescript
  if (isKyrgyz()) {
    // Логика для кыргызского языка
  }
  ```

#### `language`
- **Тип**: `string`
- **Описание**: Текущий язык приложения (read-only)
- **Пример**:
  ```typescript
  const currentLang = language; // 'ru' или 'ky'
  ```

### Пример использования в компоненте

```typescript
'use client';

import { useTranslation } from '@/hooks/useTranslation';

export default function UsersTable() {
  const { t, isRussian, getCurrentLanguage } = useTranslation();
  const lang = getCurrentLanguage();
  
  return (
    <div>
      <h1>{t('users.title')}</h1>
      <p>{t('common.loading')}</p>
      
      {isRussian() && <span>Русский интерфейс</span>}
      
      <button>{t('common.save')}</button>
    </div>
  );
}
```

---

## 4. Переключатель языка (`src/components/ui/LanguageSwitcher.tsx`)

### Описание

UI-компонент для переключения языка интерфейса. Показывает выпадающее меню с флагами стран для выбора языка.

### Основные функции

#### `handleLanguageChange(langCode: string)`
- **Описание**: Обработчик изменения языка
- **Параметры**:
  - `langCode`: Код языка (`'ru'` или `'ky'`)
- **Логика работы**:
  1. Сохраняет язык в cookie: `document.cookie = 'lang=${langCode}; path=/; max-age=31536000'`
  2. Обновляет состояние i18n: `changeLanguage(langCode)`
  3. Обновляет локальное состояние: `setCurrentLang(langCode)`
  4. Закрывает меню: `setIsOpen(false)`
  5. Обновляет страницу: `router.refresh()` (для применения SSR с новым языком)

### Код

```typescript
'use client';

import { useState, useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { useTranslation } from '@/hooks/useTranslation';
import { Icons } from '@/components/ui/Icons';

export default function LanguageSwitcher() {
  const router = useRouter();
  const { t, changeLanguage, getCurrentLanguage } = useTranslation();
  const [isOpen, setIsOpen] = useState(false);
  const [mounted, setMounted] = useState(false);
  
  // Избегаем гидратации: инициализируем язык только на клиенте
  const [currentLang, setCurrentLang] = useState('ru');
  
  useEffect(() => {
    setMounted(true);
    // Читаем язык из cookie при монтировании
    const cookieLang = document.cookie
      .split('; ')
      .find(row => row.startsWith('lang='))
      ?.split('=')[1] || 'ru';
    setCurrentLang(cookieLang);
    changeLanguage(cookieLang);
  }, [changeLanguage]);
  
  const languages = [
    { code: 'ru', name: 'RU', flag: '🇷🇺' },
    { code: 'ky', name: 'KG', flag: '🇰🇬' },
  ];
  
  const currentLanguage = languages.find(lang => lang.code === currentLang) || languages[0];
  
  // Показываем заглушку до монтирования
  if (!mounted) {
    return (
      <div className="relative">
        <button
          className="flex items-center gap-2 p-2 rounded-lg hover:bg-gray-800 transition-colors"
          disabled
        >
          <span className="text-sm text-white font-medium">RU</span>
          <Icons.ChevronDown className="h-4 w-4 text-gray-400" />
        </button>
      </div>
    );
  }
  
  const handleLanguageChange = (langCode: string) => {
    // Сохраняем язык в cookie
    document.cookie = `lang=${langCode}; path=/; max-age=31536000`; // 1 год
    
    // Обновляем состояние
    changeLanguage(langCode);
    setCurrentLang(langCode);
    setIsOpen(false);
    
    // Обновляем страницу для применения SSR с новым языком
    router.refresh();
  };
  
  return (
    <div className="relative">
      <button
        onClick={() => setIsOpen(!isOpen)}
        className="flex items-center gap-2 p-2 rounded-lg hover:bg-gray-800 transition-colors"
        data-tooltip={t('language.switch')}
      >
        <span className="text-sm text-white font-medium">
          {currentLanguage.name}
        </span>
        <Icons.ChevronDown className="h-4 w-4 text-gray-400" />
      </button>
      
      {isOpen && (
        <>
          {/* Overlay для закрытия */}
          <div
            className="fixed inset-0 z-40"
            onClick={() => setIsOpen(false)}
          />
          
          {/* Выпадающее меню */}
          <div className="absolute right-0 mt-1 w-16 bg-[#151515] rounded-xl shadow-2xl py-1 z-50 border border-gray-700/50">
            {languages.map((language) => (
              <button
                key={language.code}
                onClick={() => handleLanguageChange(language.code)}
                className={`
                  flex items-center justify-center w-full py-1 text-sm transition-colors rounded-lg m-1
                  ${currentLang === language.code 
                    ? 'bg-white text-black' 
                    : 'text-gray-300 hover:bg-gray-700 hover:text-white'
                  }
                `}
                style={{ width: 'calc(100% - 8px)' }}
              >
                <span className="text-base">{language.flag}</span>
              </button>
            ))}
          </div>
        </>
      )}
    </div>
  );
}
```

### Особенности реализации

#### Защита от гидратации (Hydration Mismatch)
- **Проблема**: Next.js выполняет рендеринг на сервере и клиенте, что может привести к несоответствию между серверным и клиентским HTML
- **Решение**: 
  1. Состояние `mounted` устанавливается в `false` по умолчанию
  2. В `useEffect` (выполняется только на клиенте) `mounted` устанавливается в `true`
  3. До монтирования показывается заглушка с дефолтным языком (`RU`)
  4. После монтирования отображается реальное состояние

#### Сохранение языка
- **Cookie**: `document.cookie = 'lang=${langCode}; path=/; max-age=31536000'`
  - `path=/` - cookie доступен для всех путей
  - `max-age=31536000` - срок жизни 1 год (в секундах)
- **Обновление страницы**: `router.refresh()` вызывается для применения SSR с новым языком

---

## 5. Middleware (`src/lib/middleware.ts`)

### Описание

Next.js Middleware обрабатывает все запросы к серверу. В контексте системы перевода он:
1. Читает язык из cookie запроса
2. Устанавливает cookie языка в ответ (если он отсутствует)
3. Сохраняет язык при редиректах

### Код (релевантные части)

```typescript
import { NextRequest, NextResponse } from 'next/server'

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl
  
  // Получаем язык из cookie или используем 'ru' по умолчанию
  const lang = request.cookies.get('lang')?.value || 'ru'
  
  // ... логика авторизации ...
  
  // Создаем ответ и устанавливаем cookie языка
  const response = NextResponse.next()
  response.cookies.set('lang', lang, { path: '/', maxAge: 31536000 }) // 1 год
  
  return response
}
```

### Функции

#### `request.cookies.get('lang')?.value`
- **Описание**: Получает значение cookie с именем `lang`
- **Возвращает**: `string | undefined` - код языка или `undefined`, если cookie не найден

#### `response.cookies.set('lang', lang, options)`
- **Описание**: Устанавливает cookie языка в ответ
- **Параметры**:
  - `'lang'`: Имя cookie
  - `lang`: Значение (код языка)
  - `options`: Опции cookie
    - `path: '/'` - путь, для которого доступен cookie
    - `maxAge: 31536000` - срок жизни в секундах (1 год)

### Важность

Middleware гарантирует, что язык сохраняется при:
- Редиректах (например, с `/login` на `/dashboard`)
- Навигации между страницами
- Перезагрузке страницы
- Первом посещении (если язык не установлен, используется `'ru'`)

---

## 6. Файлы переводов

### Структура файлов

```
src/locales/
├── ru.json  # Русские переводы
└── ky.json  # Кыргызские переводы
```

### Формат JSON

Оба файла имеют одинаковую структуру ключей, но разные значения:

```json
{
  "common": {
    "loading": "Загрузка...",  // ru.json
    "loading": "Жүктөлүүдө...",  // ky.json
    "save": "Сохранить",
    "save": "Сактоо",
    ...
  },
  "users": {
    "title": "Пользователи",
    "title": "Колдонуучулар",
    "roles": {
      "admin": "Администратор",
      "admin": "Администратор",
      ...
    }
  },
  ...
}
```

### Структура ключей

Ключи организованы в иерархическую структуру:

1. **Namespace (пространство имен)**: Группа связанных переводов
   - `common` - общие элементы (кнопки, статусы)
   - `users` - страница пользователей
   - `courses` - страница курсов
   - `sidebar` - боковое меню
   - `header` - шапка страницы
   - и т.д.

2. **Подключ**: Вложенные группы внутри namespace
   - `users.roles.admin` - роли пользователей
   - `users.complaintsModal.title` - модальное окно жалоб

3. **Финальный ключ**: Конечное значение перевода
   - `users.title` → `"Пользователи"` или `"Колдонуучулар"`

### Интерполяция переменных

i18next поддерживает интерполяцию переменных в переводах:

**JSON файл:**
```json
{
  "courses": {
    "groupsTitle": "Группы - {{courseName}}"
  }
}
```

**Использование:**
```typescript
t('courses.groupsTitle', { courseName: 'Математика' })
// Результат: "Группы - Математика"
```

### Примеры ключей

#### Общие элементы (`common`)
```json
{
  "common": {
    "loading": "Загрузка...",
    "save": "Сохранить",
    "cancel": "Отмена",
    "delete": "Удалить",
    "edit": "Редактировать",
    "create": "Создать",
    "close": "Закрыть",
    "confirm": "Подтвердить",
    "search": "Поиск...",
    "actions": "Действия"
  }
}
```

#### Пользователи (`users`)
```json
{
  "users": {
    "title": "Пользователи",
    "roles": {
      "admin": "Администратор",
      "teacher": "Преподаватель",
      "student": "Студент"
    },
    "complaintsModal": {
      "titleOnUser": "Жалобы на пользователя",
      "noComplaintsOnUser": "На этого пользователя нет жалоб",
      "complainedBy": "Жалоба от",
      "answerLabel": "Ответ на жалобу",
      "answerPlaceholder": "Введите ваш ответ...",
      "warning": "Предупреждение",
      "block": "Блокировка",
      "sendAndResolve": "Отправить и решить"
    }
  }
}
```

---

## 7. Процесс работы системы перевода

### Последовательность работы

1. **Инициализация приложения**
   ```
   RootLayout → I18nProvider → импорт i18n.ts → инициализация i18next
   ```

2. **Определение языка при первой загрузке**
   ```
   LanguageDetector проверяет в порядке:
   1. Cookie 'lang'
   2. localStorage 'i18nextLng'
   3. Язык браузера (navigator.language)
   4. HTML атрибут lang
   
   Если ничего не найдено → fallbackLng: 'ru'
   ```

3. **Использование в компонентах**
   ```typescript
   const { t } = useTranslation();
   const text = t('users.title'); // Получает перевод
   ```

4. **Переключение языка**
   ```
   Пользователь кликает LanguageSwitcher
   → handleLanguageChange('ky')
   → Сохраняется в cookie: lang=ky
   → i18n.changeLanguage('ky')
   → router.refresh()
   → Все компоненты перерисовываются с новыми переводами
   ```

5. **Сохранение языка при навигации**
   ```
   Middleware обрабатывает каждый запрос
   → Читает lang из cookie запроса
   → Устанавливает lang в cookie ответа
   → Язык сохраняется при переходах между страницами
   ```

### Жизненный цикл перевода

```
┌─────────────────────────────────────────────────────────┐
│ 1. Приложение запускается                               │
│    RootLayout монтируется                                │
│    I18nProvider импортирует '@/lib/i18n'                 │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 2. Инициализация i18next                                 │
│    Загружаются ru.json и ky.json                         │
│    LanguageDetector определяет язык:                     │
│    - Проверяет cookie 'lang'                             │
│    - Проверяет localStorage 'i18nextLng'                 │
│    - Проверяет navigator.language                        │
│    - По умолчанию: 'ru'                                  │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 3. Компонент использует перевод                          │
│    const { t } = useTranslation();                       │
│    <h1>{t('users.title')}</h1>                           │
│    i18next ищет ключ 'users.title' в текущем языке       │
│    Если не найден → использует fallbackLng               │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 4. Пользователь переключает язык                         │
│    handleLanguageChange('ky')                            │
│    → document.cookie = 'lang=ky'                         │
│    → i18n.changeLanguage('ky')                           │
│    → router.refresh()                                    │
│    → Все компоненты перерисовываются                     │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ 5. При следующем запросе                                 │
│    Middleware читает 'lang' из cookie                    │
│    Устанавливает 'lang' в ответе                         │
│    Язык сохраняется между сессиями                       │
└─────────────────────────────────────────────────────────┘
```

---

## 8. Использование в компонентах

### Базовое использование

```typescript
'use client';

import { useTranslation } from '@/hooks/useTranslation';

export default function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('users.title')}</h1>
      <button>{t('common.save')}</button>
    </div>
  );
}
```

### Использование с интерполяцией

```typescript
const { t } = useTranslation();

// JSON: { "courses": { "groupsTitle": "Группы - {{courseName}}" } }
const title = t('courses.groupsTitle', { courseName: 'Математика' });
// Результат: "Группы - Математика"
```

### Условный рендеринг по языку

```typescript
const { t, isRussian, isKyrgyz, language } = useTranslation();

return (
  <div>
    {isRussian() && <span>Русский текст</span>}
    {isKyrgyz() && <span>Кыргызча текст</span>}
    
    {/* Или */}
    {language === 'ru' ? <RussianContent /> : <KyrgyzContent />}
  </div>
);
```

### Динамическое переключение языка

```typescript
const { t, changeLanguage, getCurrentLanguage } = useTranslation();

const handleSwitch = () => {
  const current = getCurrentLanguage();
  const next = current === 'ru' ? 'ky' : 'ru';
  changeLanguage(next);
};

return (
  <button onClick={handleSwitch}>
    {getCurrentLanguage() === 'ru' ? 'Переключить на КГ' : 'Переключить на RU'}
  </button>
);
```

### Использование в серверных компонентах (Server Components)

**Важно**: Серверные компоненты Next.js не могут использовать клиентские хуки напрямую. Для работы с переводами на сервере нужно:

1. Использовать клиентские компоненты для переведенного контента
2. Передавать язык через пропсы из серверного компонента
3. Использовать `i18n` напрямую (если необходимо)

```typescript
// Server Component
import { cookies } from 'next/headers';

export default async function ServerPage() {
  const cookieStore = cookies();
  const lang = cookieStore.get('lang')?.value || 'ru';
  
  return (
    <div>
      <ClientTranslatedContent lang={lang} />
    </div>
  );
}

// Client Component
'use client';
import { useTranslation } from '@/hooks/useTranslation';

function ClientTranslatedContent({ lang }: { lang: string }) {
  const { t } = useTranslation();
  // Логика с переводами
}
```

---

## 9. Добавление новых переводов

### Шаг 1: Добавить ключ в JSON файлы

**`src/locales/ru.json`:**
```json
{
  "myFeature": {
    "title": "Мой заголовок",
    "description": "Описание функции"
  }
}
```

**`src/locales/ky.json`:**
```json
{
  "myFeature": {
    "title": "Менин аталышым",
    "description": "Функциянын сүрөттөмөсү"
  }
}
```

### Шаг 2: Использовать в компоненте

```typescript
const { t } = useTranslation();

return (
  <div>
    <h1>{t('myFeature.title')}</h1>
    <p>{t('myFeature.description')}</p>
  </div>
);
```

### Рекомендации по организации ключей

1. **Используйте namespace для группировки**
   - `users.*` - всё связанное с пользователями
   - `courses.*` - всё связанное с курсами

2. **Вложенные ключи для сложных структур**
   - `users.complaintsModal.title` - лучше, чем `usersComplaintsModalTitle`

3. **Общие ключи в `common`**
   - `common.save`, `common.cancel`, `common.loading`

4. **Избегайте слишком глубокой вложенности**
   - `users.complaintsModal.title` - хорошо
   - `app.dashboard.users.complaintsModal.title` - слишком глубоко

---

## 10. Отладка и решение проблем

### Проблема: Перевод не работает

**Причины:**
1. Ключ не найден в JSON файле
2. Неправильный путь к ключу
3. Язык не инициализирован

**Решение:**
```typescript
const { t, language } = useTranslation();

// Проверьте текущий язык
console.log('Current language:', language);

// Проверьте наличие ключа
console.log('Translation:', t('your.key'));

// Включите debug режим в i18n.ts
i18n.init({
  debug: true, // Включит логирование
  ...
});
```

### Проблема: Гидратация (Hydration Mismatch)

**Причина:** Несоответствие между серверным и клиентским рендерингом

**Решение:**
```typescript
const [mounted, setMounted] = useState(false);

useEffect(() => {
  setMounted(true);
}, []);

if (!mounted) {
  return <div>Loading...</div>; // Заглушка
}

return <div>{t('translation.key')}</div>;
```

### Проблема: Язык не сохраняется

**Причина:** Cookie не устанавливается правильно

**Решение:**
1. Проверьте middleware - он должен устанавливать cookie в ответе
2. Проверьте `LanguageSwitcher` - он должен сохранять в cookie
3. Проверьте настройки `detection` в `i18n.ts`

---

## 11. API методы i18next

### `i18n.changeLanguage(lng)`
- **Описание:** Меняет текущий язык
- **Параметры:** `lng` - код языка (string)
- **Пример:** `i18n.changeLanguage('ky')`

### `i18n.language`
- **Описание:** Текущий язык (read-only)
- **Тип:** `string`
- **Пример:** `console.log(i18n.language)` // 'ru' или 'ky'

### `i18n.t(key, options)`
- **Описание:** Функция перевода (используется напрямую, без хука)
- **Параметры:**
  - `key`: Ключ перевода (string)
  - `options`: Опции (object) - переменные для интерполяции
- **Пример:** `i18n.t('users.title', { name: 'John' })`

### `i18n.addResourceBundle(lng, ns, resources, deep, overwrite)`
- **Описание:** Динамически добавляет переводы
- **Использование:** Редко, обычно переводы загружаются статически

### `i18n.getFixedT(lng, ns)`
- **Описание:** Возвращает функцию `t`, зафиксированную для конкретного языка
- **Пример:** `const t = i18n.getFixedT('ru'); t('users.title')`

---

## 12. Сводная таблица файлов и их ролей

| Файл | Роль | Тип | Когда используется |
|------|------|-----|-------------------|
| `src/lib/i18n.ts` | Конфигурация i18next | Конфигурация | При импорте (инициализация) |
| `src/components/providers/I18nProvider.tsx` | Провайдер i18n | React Component | В RootLayout (обертка приложения) |
| `src/hooks/useTranslation.ts` | Хук для переводов | React Hook | В любом клиентском компоненте |
| `src/components/ui/LanguageSwitcher.tsx` | Переключатель языка | React Component | В Header/DashboardLayout |
| `src/lib/middleware.ts` | Middleware для языка | Next.js Middleware | При каждом запросе к серверу |
| `src/locales/ru.json` | Русские переводы | JSON | При загрузке i18n |
| `src/locales/ky.json` | Кыргызские переводы | JSON | При загрузке i18n |

---

## 13. Заключение

Система перевода в проекте построена на базе **i18next** и **react-i18next**, что обеспечивает:

✅ **Простое использование** - хук `useTranslation()` в любом компоненте  
✅ **Автоматическое определение языка** - из cookie, localStorage, браузера  
✅ **Сохранение выбора** - язык сохраняется между сессиями  
✅ **Типобезопасность** - TypeScript поддерживает типизацию ключей  
✅ **Интерполяция** - поддержка переменных в переводах  
✅ **SSR совместимость** - работает с Next.js Server Components  
✅ **Легкое расширение** - простое добавление новых языков и ключей  

Система полностью функциональна и готова к использованию во всех компонентах приложения.

