# 🚀 Инструкция по настройке Bilimpoz Teacher

## 📋 Предварительные требования

- Node.js 18+ 
- PostgreSQL база данных
- npm или yarn

## ⚙️ Пошаговая настройка

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка базы данных

Создайте файл `.env` в корне проекта:
```env
DATABASE_URL="postgresql://username:password@localhost:5432/bilimpoz_teacher?schema=public"
```

Замените `username`, `password`, `localhost:5432` и `bilimpoz_teacher` на ваши данные.

### 3. Инициализация Prisma

```bash
# Генерация Prisma Client
npm run db:generate

# Применение схемы к базе данных
npm run db:push
```

### 4. Запуск проекта

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build
npm run start
```

## 🌐 Доступ к приложению

После запуска приложение будет доступно по адресу:
- **Development**: http://localhost:3000
- **Production**: ваш домен

## 🗄️ Структура базы данных

Проект использует существующую схему Bilimpoz с основными таблицами:

### Пользователи и профили
- `users` - основная информация о пользователях
- `user_stats` - статистика активности
- `user_data` - дополнительные данные профиля
- `social_networks` - социальные сети

### Образовательный контент
- `courses` - курсы
- `lessons` - уроки
- `questions` - вопросы
- `mentor_lessons` - уроки преподавателей

### Коммуникация
- `discussions` - обсуждения
- `discussion_chat` - сообщения в чатах
- `complaints` - жалобы

### Система баллов и подписок
- `points_transactions` - транзакции баллов
- `subscriptions` - подписки
- `user_subscriptions` - активные подписки пользователей

## 🎯 Основные функции

### 🏠 Главная страница
- Статистика преподавателя (активные ученики, уроки, вопросы)
- Фильтрация по периодам
- Быстрые действия

### ❓ Страница вопросов
- Просмотр всех вопросов учеников
- Фильтрация по статусам (новые, отвеченные, проблемные)
- Ответы на вопросы
- Управление статусами

### 💬 Обсуждения
- Чат с учениками в реальном времени
- Управление активными обсуждениями
- Закрытие завершенных обсуждений
- AI суммаризация (заготовка)

### ⚙️ Настройки
- Редактирование профиля
- Управление социальными сетями
- Смена пароля
- Настройка языка интерфейса

## 🎨 Дизайн и стили

Проект следует дизайн-системе Bilimpoz Admin:

### Цвета
- Основной фон: `#0b0b0b`
- Карточки: `#151515`
- Элементы: `#242424`
- Текст: `#ffffff`, `#9ca3af`, `#6b7280`

### Компоненты
- Все компоненты следуют единому стилю
- Темная тема с высоким контрастом
- Плавные анимации и переходы
- Адаптивный дизайн

## 🔧 Полезные команды

```bash
# Просмотр базы данных
npm run db:studio

# Проверка кода
npm run lint

# Сброс базы данных (осторожно!)
npx prisma db push --force-reset
```

## 🚨 Важные замечания

1. **База данных**: Убедитесь, что PostgreSQL запущен и доступен
2. **Переменные окружения**: Не забудьте создать файл `.env`
3. **Порты**: По умолчанию используется порт 3000
4. **Безопасность**: В продакшене используйте сильные пароли и HTTPS

## 🆘 Решение проблем

### Ошибка подключения к БД
```bash
# Проверьте правильность DATABASE_URL в .env
# Убедитесь, что PostgreSQL запущен
# Проверьте права доступа к базе данных
```

### Ошибки Prisma
```bash
# Переустановите Prisma Client
npm run db:generate

# Сбросьте схему (осторожно!)
npx prisma db push --force-reset
```

### Ошибки сборки
```bash
# Очистите кэш
rm -rf .next node_modules
npm install
npm run build
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера
2. Проверьте логи сервера в терминале
3. Убедитесь в правильности настройки базы данных
4. Проверьте версии Node.js и npm

---

**Готово!** 🎉 Ваш сервис для преподавателей Bilimpoz готов к использованию.






